<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DistribuidoraFabio.Vendedor.ListaVendedor"
             Visual="Material"
             BackgroundColor="White"
             Title="Lista de vendedores">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Agregar" Clicked="ToolbarItem_Clicked"/>
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <ListView x:Name="listaVendedor" ItemTapped="OnItemSelected" SeparatorColor="#465B70">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding nombre}" TextColor="#000000" Detail="{Binding telefono}" DetailColor="#465B70"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentPage.Content>
</ContentPage>